### <요약>
- 스프링 Data JPA는 리턴 타입을 무엇으로 지정하느냐에 따라, 해당 타입을 바꿔준다

### 1. 값이 있는 경우 반환 타입
- 예시(repository1)
    - 모두 같은 이름의 함수이지만, 리턴 타입에 따라 가져오는 값을 바꿔줌

### 2. 값이 없는 경우 반환 타입
- 예시(repository2)
    - List는 빈 리스트로 만들어짐
    - 단일 객체는 NULL
    - (DB에 XXX라는 이름은 없는 상황)

### 3. 중복된 객체 조회
- 예시(repository3)
    - a 이름은 2개인데, 1개의 객체만 조회
    - ⇒ 에러
