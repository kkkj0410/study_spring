- 설명
    - JPA에서는 Repository에 관한 기능들을 구현해놓았다
    - 따라서, 해당 JpaRepository를 상속받아서 별도의 정의 없이 repository를 사용 가능하다

- 예시
    - 해당 interface를 상속할 필요 없이, 그대로 MemberRepository로 선언해서 사용 가능
    - 만들고자 하는 함수는 interface 안에서 이름에 따라 기능이 정해진다.
