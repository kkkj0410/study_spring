### 임베디드 타입의 문제점?

- 객체 a, b가 있다
- a, b는 동일한 임베디드 타입의 객체를 지닌다.
- 이때, 임베디드 타입 객체의 값을 바꾸면, a,b가 전부 바뀐다
- 문제점
    - 의도치 않게, 임베디드 타입의 값이 수정되면 해당 타입을 지닌 모든 객체의 값이 바뀐다
    - 컴파일 오류가 일어나지 않으므로, 해당 원인을 찾기 어렵다.

- 해결법
    - 임베디드 타입의 객체 수정을 막는다
    - = setter함수를 없앤다
    - Adress 코드를 보면, 처음 생성 이후 값 수정이 불가하다.
