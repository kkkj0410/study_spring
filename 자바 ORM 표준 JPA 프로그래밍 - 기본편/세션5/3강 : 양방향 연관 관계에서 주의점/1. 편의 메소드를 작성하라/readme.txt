### 1. 편의 메소드 작성

진짜 매핑(연관관계의 주인)에만 값을 반영한다면?

- member.setTeam(team);
- 가짜 매핑에게도 해당 변경 사항이 적용됨
    - 이유는 DB 조회를 통해, 변경 사항이 반영되기 때문
- 하지만 가짜 매핑의 값도 자바 코드 상에서 수정해야함
    - 이유
        - DB를 사용하지 않는 테스트 코드에서도 정상적으로 작동하기 위함
        - SQL 쿼리가 나가지 않은 상황에서는 진짜 매핑의 수정 사항이 가짜 매핑에게 전달되지 않기 때문
